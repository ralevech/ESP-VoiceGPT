// ====================================================================
// globals.h - Глобальные переменные проекта
// Автор: ralevech
// ====================================================================

#ifndef GLOBALS_H
#define GLOBALS_H

#include <Arduino.h>

// ====================================================================
// СИСТЕМНЫЕ ФЛАГИ
// ====================================================================

extern bool ledState;       // Состояние встроенного LED (true=вкл, false=выкл)
extern bool apMode;         // Режим точки доступа (true=AP, false=клиент)
extern bool serverRunning;  // Статус веб-сервера (true=запущен)
extern bool audioReady;     // Статус аудио (true=инициализировано)

// ====================================================================
// ДАННЫЕ С ДАТЧИКОВ
// ====================================================================

extern float lastTemperature;  // Последнее измеренное значение температуры

#endif